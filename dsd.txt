 private long? GetRoleAccessMapping(IRBADatabaseEntities dbDatabase, string roleName, string screenName, string accessName)
 {
     var roleAccess = dbDatabase.Role_Access_Mapping
         .FirstOrDefault(r => r.RoleName == roleName && r.ScreenName == screenName && r.AccessName == accessName);

     return roleAccess?.Id;
 }
